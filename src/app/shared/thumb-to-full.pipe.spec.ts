import { ThumbToFullPipe } from './thumb-to-full.pipe';

describe('ThumbToFullPipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbToFullPipe();
    expect(pipe).toBeTruthy();
  });
});
